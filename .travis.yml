sudo: required
services:
  - docker

before_install:
  - docker build -t jpbarbosa/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jpbarbosa/docker-react npm run test -- --coverage
